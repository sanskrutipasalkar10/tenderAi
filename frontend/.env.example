# Base URL of the backend API (backend/app/main.py). Browser-visible (NEXT_PUBLIC_
# prefix, baked in at build time) — never put a secret here, there isn't one to put:
# auth is the single shared JWT credential entered at /login, not a build-time key.
NEXT_PUBLIC_API_URL=http://localhost:8000
