# Frontend (Next.js + Tailwind)

Built in **Phase 8** per the implementation plan — upload flow, per-document tabbed view
(Go/No-Go, Synopsis, Risk Finder), and the `CitationLink` component that lets a user
click any `page_ref` to view the source page side-by-side. That citation-click-through is
this project's HITL mechanism (see `docs/SPEC.md` §12 HITL note) — there is no separate
approve/override workflow to build here.

Not scaffolded yet — backend Phases 0–7 come first.
