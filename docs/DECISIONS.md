# Decision Log — Tender AI Platform

Add a row every time a technical choice is made. This is the difference between "I built
a tender-analysis pipeline" and "I chose X over Y because Z, measured how."

| # | Decision | Options considered | Chose | Why | Revisit if |
|---|---|---|---|---|---|
| 1 | Backend language | Python/FastAPI, Node/Fastify | Python/FastAPI | Confirmed with user; PyMuPDF/pdfplumber/Camelot are Python-native, keeps pipeline one language | Never without explicit new ask |
| 2 | Project tier | 1 / 2 / 3 | Tier 2 — Internal/Pilot | Internal bid-team users only; blast radius is "bad internal decision," not customer-facing | Pilot expands externally or multi-org |
| 3 | HITL mechanism | Full approve/override + correction table vs. citation-verification UI | Citation-verification UI | Every output already carries `page_ref` per spec's own success criteria; satisfies Tier 2's "if high-stakes" HITL requirement without new schema | Users start needing systematic correction feedback into prompts/evals |
| 4 | Architecture shape | Pipeline / single agent+tools / multi-agent | Pipeline (Celery map-reduce) | All stages deterministic and fixed; no runtime branching decision exists; not RAG, no retrieval loop to wrap in an agent | A future feature needs dynamic tool selection |
| 5 | LLM provider access | Direct SDK calls per stage vs. LiteLLM wrapper | LiteLLM via `app/llm/client.py` | Keeps "swap model in a day" true; centralizes retry/backoff/timeout once instead of 3x | Never — low-cost even at small provider count |
| 6 | Job orchestration | Celery+Redis / RQ+arq / FastAPI BackgroundTasks | Celery + Redis | Spec requires async per-chunk retries, not one long sync request; chord/group maps directly onto map(fan-out)→reduce(fan-in) | Pipeline complexity shrinks enough a simpler queue suffices |
| 7 | Auth | JWT / OAuth-Clerk / NextAuth | JWT via FastAPI `security.py` | Tier 2 requires JWT/OAuth; no social-login need stated; internal users provisioned directly | Pilot needs SSO with company IdP |
| 8 | Tracing tool | LangSmith / Langfuse | Langfuse | LangSmith's edge is native LangChain/LangGraph integration; this project uses neither | Project later adopts LangGraph for a genuinely agentic feature |
| 9 | Eval harness | Ragas / custom pytest+golden JSONL | Custom pytest (DeepEval-style) over `evals/datasets/*.jsonl` | Ragas metrics are retrieval-specific; no retrieval step exists here | A future retrieval feature (chat-over-tender) ships |
| 10 | `pgvector` extension | Enable now unused / enable later | Enable now, no vector columns in MVP | Per spec §2.7 — avoids disruptive migration later, zero cost now | Never — already decided in source spec |
| 11 | Object storage | MinIO local / direct cloud S3 in dev too | MinIO local → S3/R2 prod | Per spec §2.2/2.6 — S3-compatible API, zero code change on migration | Prod target changes away from S3-compatible storage |
| 12 | Vision extraction provider | Gemini-only / Ollama-only / Gemini primary+Ollama fallback | Gemini free tier primary, `qwen2.5vl:7b` via Ollama (`USE_LOCAL_VISION=true`) | Gemini also does reduce pass (needs vision anyway), free-tier no card; Ollama covers offline/quota-exhaustion | Gemini free tier discontinued, or pilot needs air-gapped operation |
| 13 | Migration tool | Alembic vs. hand-run DDL | Alembic, DDL from spec §3.1 applied verbatim in the first migration | Spec explicitly calls for a migration tool from day one, not hand-run DDL against a long-lived DB | Never |
| 14 | Golden eval set v1 source | Wait for real tender data vs. build synthetic now | Synthetic, programmatically generated (`scripts/build_eval_set.py`) — 23 small PDF fixtures, 144 golden examples across 5 datasets | Phase 1 must exist before Phase 2 starts per the Build Kit; real tender/company-profile examples weren't available yet. Fixtures are genuine PDFs (real text layers / real image-only scans / real ruled tables), not just JSON metadata, so Phase 2's classifier is tested against real extraction behavior, not labels alone | Real anonymized tender excerpts arrive — add as further fixtures + golden rows via the same generator/format, do not replace it |
| 15 | GitHub repo hosting | Private vs. public | Public, under `sanskrutipasalkar10/tenderAi` | User's explicit choice | User asks to make it private |
| 16 | Local dev auth for git/gh | PAT (fine-grained or classic) vs. OAuth device flow | gh's own OAuth device flow, scopes `repo,workflow` | Fine-grained PATs repeatedly denied push despite correct Contents permission (root cause: pushing `.github/workflows/*` requires the separate `workflow` scope, easy to miss on a fine-grained token); device flow with explicit scopes worked on the first correctly-scoped attempt | Never — this is just how repo auth was bootstrapped on this machine |
| 17 | Reconciling colleague's DB schema | (a) adopt colleague's bronze/silver/gold schema as-is, accept page-*range* citations; (b) push back and ask colleague to add page-level granularity; (c) extend colleague's schema myself; (d) keep our exact-page schema as canonical and additionally populate colleague's 4 tables as a derived export/reporting layer | (d) — colleague's schema stays untouched/verbatim, our page-level schema (documents/pages/chunks/chunk_extractions/document_analysis/boilerplate_cache) remains the system of record; a sync step populates `company_master_profile`/`tender_bronze_raw`/`tender_silver_extracted`/`tender_gold_analysis` from it | Colleague's schema only tracks a `page_range` string per chunk (e.g. "Pages 1-20") — no way to cite one exact page. This breaks the spec's own MVP success criterion ("every risk flag citing a real page... verifiable") and the citation-click HITL mechanism (SPEC.md §12 HITL note). Colleague's schema also has no documents-lifecycle table, no dedupe cache, and no dedicated synopsis field. User chose to keep both: full fidelity internally, org-compatible export externally. Mapping detail: `docs/ARCHITECTURE.md` §Bronze/Silver/Gold export layer | Colleague's schema gains page-level granularity of its own — the export layer could then become the single schema |
| 18 | Page classification signals | PyMuPDF-only heuristic vs. calling pdfplumber's table-finder inside classify.py | PyMuPDF-only (text length + embedded-image presence + ruled-line grid counting via `page.get_drawings()`) | Keeps classify.py self-contained (needs only the one fitz.Page already in hand) and fast — no LLM call and no second library round-trip for what's meant to be the cheap first gate (docs/DECISIONS.md #4). pdfplumber's real table-detection is still used, just downstream in `extract_native.extract_table_structure`, which genuinely needs its cell-extraction algorithm | Grid-line heuristic proves unreliable on real (non-ruled/borderless) tender tables — revisit with real data in Phase 1's dataset-enrichment pass |
| 19 | Testing the ingestion layer without a live Postgres | Real integration tests now vs. mocked-DB unit tests now, real integration tests later | Mocked-DB unit tests now (`FakeSession` in test_ingestion.py, dependency-override mocks in test_routes_ingest.py) — real content (fixture PDFs) flows through classify/extract for real, only persistence is faked | No Postgres available yet (colleague's instance pending; Docker unavailable in this dev environment). Faking the DB layer honestly, rather than skipping these tests, still catches real bugs in the orchestration logic and the real extraction functions | Colleague's Postgres credentials arrive — add tests/integration/ tests against the real DB before Phase 2 is considered fully closed |
| 20 | S3 client `endpoint_url` handling | Always pass `settings.s3_endpoint_url` vs. omit when falsy | Omit when falsy (`get_s3_client()` only sets `endpoint_url` if configured) | moto's `mock_aws` only intercepts AWS's own default endpoints — a custom endpoint_url (MinIO-style) bypasses its interception and tries a real network call. Omitting it lets tests monkeypatch the setting empty and get real moto mocking; production always sets it via `.env` | Never — this is purely a testability accommodation, zero behavior change in real runs |
| 21 | Reaching the shared dev Postgres | Direct LAN/localhost vs. Tailscale (colleague's machine is remote, not on the same network) | Tailscale — connected via the `c4i4.org`/`tailscale.constantip` tailnet, host `100.65.111.7` | Colleague's Postgres runs on her own machine, not a shared server; Tailscale was already the team's chosen mesh-VPN for this | If the DB moves to a proper shared/cloud instance, this whole detour goes away — `DATABASE_URL` just points elsewhere |
| 22 | `pgvector` extension — real-world gap | Require it (fail migration if absent) vs. attempt + continue on failure | Attempt via a `SAVEPOINT`-wrapped statement, continue on failure with a warning | Confirmed via `pg_available_extensions` that pgvector isn't installed on the colleague's real Postgres 18 instance — only `pgcrypto` is. No MVP table/column uses `vector` (spec §2.7), so this is safe to defer rather than block Phase 2 on installing a binary extension nobody needs yet | The chat-over-tender feature (out of MVP scope) is actually greenlit — pgvector needs to be genuinely installed on whichever Postgres hosts prod by then |
| 23 | Migration 0002 idempotency | `CREATE TABLE` (as colleague wrote it) vs. `CREATE TABLE IF NOT EXISTS` | `IF NOT EXISTS`, columns/types/constraints otherwise untouched | The shared dev database already had `company_master_profile` (with 1 real row) created independently before our migration first ran against it — a plain `CREATE TABLE` would have failed the whole migration | Never — this is a safety guard, not a schema change, and is harmless even once nothing pre-exists |
| 24 | Model registration for foreign-key resolution | Import each model module individually where needed vs. import-everything in `app/models/__init__.py` | `app/models/__init__.py` imports every model module, so importing the package (which happens implicitly any time any single model is imported) always registers the full `Base.metadata` graph | Real bug hit during the first live-Postgres integration test: `Document.company_profile_id`'s FK to `company_profiles` failed with `NoReferencedTableError` because only `document`/`page` had been imported, not `company_profile` — SQLAlchemy only knows about tables whose model class has actually been imported somewhere in the process | Never — this is the correct general fix, not a workaround for one test |
| 25 | `classify.py`'s table-grid line-length threshold | 10pt (original) vs. recalibrated against real documents | 60pt (`MIN_LINE_LENGTH`) | Ran the classifier against 8 real Indian government tender PDFs (1197 pages, `documents/`). A BHEL tender's letterhead page has a legacy vector-embedded Tamil-script font whose glyphs are drawn as tiny line strokes (10-45pt, confirmed by inspecting `page.get_drawings()` directly) — 10pt let this through and false-positived the letterhead as a "table". Real table borders on the same document measured 495-755pt. 60pt keeps margin above the measured noise ceiling while still clearing small legitimate tables (a 2-3 row synthetic fixture's vertical border is ~78pt) — both the golden eval (100%, 109/109) and the real-document false positive were re-verified after the change | Another real document produces line-art noise longer than 60pt — consider a page-relative percentage instead of an absolute point value (noted as residual risk in classify.py's own comment) |
| 26 | `extract_table_structure`'s header-row detection | Always treat row 0 as the header vs. detect the real header row | Skip leading "preamble" rows (single populated cell, e.g. a section title or enquiry number spanning the row) to find the first "dense" row; if that row's first cell is a bare serial number (`_looks_like_data_row`), treat it as a continuation page with no header of its own rather than mislabeling a data row as a header | Found on the same 8-real-document run: BHEL's 135 real BOQ-style tables have 1-3 title/metadata rows before the actual column-header row, confirmed by inspecting pdfplumber's raw row output directly. The naive "row 0 = header" assumption produced a title fragment as "headers" on 130/135 tables. Multi-page tables also repeat the SAME preamble on every continuation page without repeating the real header row — an earlier, looser fix (any row with >=half its columns populated is "the header") wrongly claimed a data row as the header on those pages; the serial-number check fixes that specifically. Re-verified: 69 pages now get a real detected header, 69 are correctly flagged as headerless continuations (all rows preserved as data, none dropped), 0 pages lose all their rows, 2715 total data rows captured — up from a state where 130/135 tables had a misleading header and this count wasn't even being measured correctly | A table's real column-header row doesn't start each section on a fresh page (i.e. this pagination pattern isn't universal) — this is inherently a per-page heuristic; genuine cross-page table-continuation tracking is a Phase 4 concern (chunking has multi-page context this doesn't) |
| 27 | Committing real tender PDFs (`documents/1-8`) to git | Commit as-is (repo is public) / make repo private then commit / never commit, local-only | Never commit — `documents/*.pdf` added to `.gitignore` | These are real, business-sensitive procurement documents (not synthetic fixtures) used to validate the pipeline against real data; the repo is public. Regardless of visibility, committing them isn't necessary for anyone else to build/run this project — the synthetic golden set already serves that purpose | User decides these specific documents are fine to share (e.g. for a demo) — even then, reconsider per-document rather than blanket-committing the directory |
| 28 | Vision/map/reduce LLM provider | Gemini + Groq (original spec plan) vs. Ollama Cloud (user's explicit request) | Ollama Cloud, single provider: `gpt-oss:20b-cloud` (map), `gpt-oss:120b-cloud` (reduce), `gemma4:cloud` (vision) | User asked to use Ollama Cloud models. Verified free-tier availability by direct testing (`ollama run <model>:cloud`) — the large/flagship models (`qwen3.5:397b-cloud`, `minimax-m3:cloud`) returned 402 Payment Required on this account, but `gpt-oss:20b-cloud`, `gpt-oss:120b-cloud`, and `gemma4:cloud` all worked for free. `gemma4:cloud` was then confirmed genuinely vision-capable by sending it a real scanned tender page and getting a correct, detailed transcription back — not just present in Ollama's "vision" search tag. Simpler than juggling 3 providers (Gemini/Groq/Ollama-fallback) for no cost difference on this account | Free-tier limits on this account change, or a task needs a capability none of these 3 models have |
| 29 | litellm's Ollama image handling | Use litellm's `ollama`/`ollama_chat` providers for vision calls vs. bypass litellm for image-bearing Ollama calls specifically | Bypass — `_complete_ollama_vision_native` in `app/llm/client.py` calls Ollama's native `/api/chat` directly, only when an image is present and the target is Ollama | Verified real bug in litellm 1.56.5: the `ollama/` provider (legacy `/api/generate`) stringifies the whole OpenAI-style multimodal `content` list into the prompt via Python's dict repr — the model receives a literal `"[{'type': 'image_url', ...}]"` string, not an image, and reports seeing nothing. The `ollama_chat/` provider (`/api/chat`) sends the same content list as-is, which Ollama's API schema rejects with 400 (it requires `content: str` + a separate top-level `images: [base64...]` array). Confirmed via debug logging (raw request payloads) and by calling Ollama's native API directly with the correct shape, which worked immediately. Tried upgrading litellm to 1.101.0 to see if it was fixed there — it was, presumably, but that version requires a newer pydantic than this project is pinned to (`PydanticSchemaGenerationError` on import) and upgrading pydantic risks cascading into fastapi/pydantic-settings compatibility issues, not worth it for one call path | A litellm version compatible with this project's pinned pydantic fixes this — remove the workaround and go back through litellm for consistency |
| 30 | Vision extraction render DPI | 150 DPI (initial attempt) vs. 100 DPI | 100 DPI | A real scanned tender page at 150 DPI produced a base64-encoded image large enough to overflow the model's 262K-token context window (`"prompt is too long: 381989"`, confirmed via direct error message). 100 DPI (191KB PNG) worked and the resulting transcription was accurate and complete (verified against the actual rendered image — a vendor-approval table with 7 rows of company names/addresses/phone numbers, all correctly transcribed including a markdown table structure) | A real document needs higher resolution to stay legible (very small print) — consider per-page adaptive DPI instead of a fixed constant |
| 31 | Deleting a document that seeded a boilerplate_cache row | Add `ON DELETE CASCADE` to `boilerplate_cache.first_seen_document_id` (schema change) vs. leave the verbatim DDL as-is and handle it at the application/cleanup layer | Leave the DDL as-is; delete/null the referencing `boilerplate_cache` row(s) before deleting the document | Real FK constraint violation hit by `tests/integration/test_ingestion_integration.py`'s cleanup once Phase 3's dedupe started actually writing to `boilerplate_cache` (Phase 2 never exercised this FK — the table was always empty). `boilerplate_cache` is explicitly documented as "not tied to one document" (spec §3.2) — a cross-document cache entry silently disappearing because one contributing document was deleted is arguably wrong behavior, so this is likely intentional, not an oversight, and shouldn't be casually cascaded away | A real document-deletion feature is built (Tier 2 data-governance requirement, docs/SPEC.md) — this needs an explicit, deliberate decision then: null the FK (keep the cache entry, lose the provenance pointer) or require re-attribution to another document that also saw the same content |
| 32 | Cloud model resilience | Let a cloud outage/rate-limit fail the pipeline vs. automatic cloud->local fallback | Automatic fallback — `app.llm.client.complete_for_task(task, ...)` tries the Ollama Cloud primary first, and on `ProviderError` (after its own bounded retries) falls back to a genuinely local Ollama model for the same task, returning which model actually served the request so provenance (`pages.extraction_method`) stays accurate | User asked: "if cloud models are not working properly turn to ollama local models". Local fallback models: `qwen2.5-coder:7b` for map/reduce (already available locally; a code-tuned model isn't ideal for general reasoning, but "works" beats "blocked" for a fallback path that should rarely trigger), `qwen2.5vl:7b` for vision (the model docs/SPEC.md §2.4 originally specified — pulled specifically for this). Verified end-to-end for real (not just mocked): forced a genuine primary failure (nonexistent cloud model tag → real 404) and confirmed the local fallback correctly took over and returned a correct response | The local fallback models prove inadequate quality-wise once Phase 4/5 needs structured JSON output from them — evaluate a better-suited local model for map/reduce specifically |
| 33 | Chunk token-count estimation | `tiktoken` (real BPE tokenizer) vs. a simple chars/4 heuristic | chars/4 heuristic | `tiktoken.get_encoding("cl100k_base")` fetches its BPE file over the network on first use — hit a real multi-minute hang running the unit tests the first time, an unacceptable non-deterministic dependency for what's only a rough chunk-sizing estimate (CLAUDE.md hard rule 8: tests must be deterministic, $0). chars/4 is the standard rough approximation for English text and needs no download, no network, no per-provider tokenizer distinction | Chunk sizing needs to be precise against a specific provider's real tokenizer (e.g. chunks are overflowing/underflowing a model's context window in practice) — revisit with a pinned, pre-downloaded tokenizer file bundled in the repo, not a runtime fetch |
| 34 | LiteLLM's `timeout` enforcement for Ollama | Keep routing text calls through litellm.completion() vs. route ALL Ollama calls (not just image ones) through the native /api/chat, bypassing litellm entirely | Bypass litellm entirely for every Ollama call — `complete()` now always calls `_complete_ollama_native`, which uses `requests` with its own enforced timeout | Real, serious bug: a genuine 25-page map-pass chunk (~17K tokens) sent via `litellm.completion(model="ollama_chat/...", timeout=45)` hung well past 45s with no error — confirmed by direct reproduction, `requests.post(..., timeout=X)` on the exact same payload against Ollama's native `/api/chat` fails cleanly at exactly X seconds every time. This is a second, more serious litellm/Ollama integration gap after #29 (image handling) — an unenforced timeout is a direct CLAUDE.md hard-rule-10 violation (every external call must have a timeout), not just an inconvenience. litellm stays imported/available for a hypothetical future non-Ollama provider, but 100% of current traffic bypasses it | A litellm upgrade (compatible with this project's pinned pydantic — see #29) is confirmed to enforce Ollama timeouts correctly — revisit whether the native-API path is still worth the added code vs. going back through one library |
| 35 | Chunk size (CHUNK_SIZE_PAGES) | Spec's own starting suggestion (~20-30 pages) vs. a much smaller size based on measured throughput | 5 pages (down from an initial 25), overlap 1 page (down from 2, proportionate to the smaller chunk) | Direct measurement, not guesswork: a real 25-page chunk (~17K tokens, real BHEL/Catering tender text, the actual map-pass extraction prompt) failed to complete in repeated attempts up to an 8-minute timeout against gpt-oss:20b-cloud. A 10-page chunk didn't complete in 90s. A 3-page chunk completed in 79s. This is a real throughput ceiling for this task's complexity (long 4-category extraction instructions + strict JSON output), confirmed on both the cloud model and the local qwen2.5-coder:7b fallback (which was, if anything, slower) — not something a longer timeout alone fixes, and not specific to network/cloud queueing. More chunks means more map-pass calls per document (proportionately more total processing time and more Celery tasks), which the pipeline's async, per-chunk-retry design already accommodates | A latency sweep across 4-15 page chunk sizes finds a better size/throughput balance, or a genuinely faster model becomes available for the map task — 5 pages is a conservative starting point, not a proven optimum |
| 36 | Map-pass call timeout/retry defaults | Keep the original 60s/3-retries default vs. raise the timeout and lower the retry count, given real observed latency | `DEFAULT_TIMEOUT_SECONDS=180`, `DEFAULT_NUM_RETRIES=1` (down from 3 — Celery's own per-chunk task retry in tasks_map.py is a second, coarser retry layer; compounding two aggressive retry counts multiplies worst-case wait time) | Same latency measurements as #35 — even at the now-much-smaller 5-page chunk size, a 3-page-equivalent call still took 79s, so a generous per-call timeout is still needed even after the chunk-size fix. A call that never returns at all (the #34 bug, now fixed) is a different problem from a call that's legitimately just slow | A latency sweep (see #35) settles on a chunk size fast enough that these defaults can come back down |
| 37 | Prompt content substitution | `str.format(content=...)` vs. `.replace("{content}", ...)` | `.replace()` | Real bug: the map-pass prompt file's own JSON example (`{"dates": [...], ...}`) is full of literal `{braces}` — `.format()` tried to interpret every one of them as a placeholder and raised `KeyError` on the first real call. `.replace()` only touches the literal `{content}` token, leaving the rest of the prompt (including JSON examples) untouched. Documented in app/prompts/registry.py's module docstring so reduce_pass.py's prompts (Phase 5), which will have the same JSON-example shape, don't repeat the mistake | Never — `.replace()` is strictly safer for any prompt that might contain example JSON |
| 38 | company_profile required-fields list (go_no_go) | Guess a plausible list vs. derive it from the golden adversarial fixture | `company_name`, `annual_turnover`, `certifications`, `sectors`, `max_capacity_pct` must be non-null; `past_projects`/`geographic_presence` may be empty/short without triggering a gap | evals/datasets/golden_adversarial.jsonl's incomplete_company_profile case is unambiguous ground truth: `profile_incomplete` has `certifications: null` and `sectors: null` (both expected as gaps) while `past_projects: []` and a 1-item `geographic_presence` are NOT flagged as gaps, even though the qualified profile has more of each. Resolves the plan's own open decision ("company_profile required-fields list") from real fixture evidence instead of a guess. A profile missing any required field short-circuits go_no_go to `Conditional-Go` before any LLM call — no cost spent scoring against data we already know is incomplete | Domain expert specifies a different required-fields list, or wants a distinction between "field absent" and "field present but empty" |
| 39 | Risk severity rubric + risk_score formula | Let the reduce-pass model assign severity/score directly vs. a code-based rubric computed from the model's (narrower) category output | Code-based: `SEVERITY_BY_CATEGORY` maps category -> HIGH/MEDIUM/LOW (Liquidated Damages/Indemnity=HIGH, Payment Terms/Termination=MEDIUM, Force Majeure=LOW, unrecognized category defaults to MEDIUM with a logged warning); `risk_score = min(100, sum(weight[severity] for each risk))` with weights HIGH=30/MEDIUM=15/LOW=5. The reduce-pass model itself only returns category/clause_summary/page_ref (consolidated/deduped from map-pass risk_candidates) | CLAUDE.md hard rule 3 ("score-formula math are code, not prompt instructions") plus the plan's own open decision ("risk severity thresholds... needs domain-expert input... encode as an explicit rubric, not model discretion"). The category->severity mapping isn't a guess: it's 100% consistent across every row of evals/datasets/golden_risk_finder.jsonl, i.e. real evidence of the intended rubric, not an invented one. The weighted-sum score formula is a simple, explainable placeholder pending real domain-expert-set weights | A domain expert supplies different category weights, wants a non-linear formula, or a new risk category doesn't fit the existing HIGH/MEDIUM/LOW rubric cleanly |
| 40 | go_no_go decision rule, citation re-verification, and document_analysis upsert | (a) Model decides Go/No-Go directly vs. code computes it from per-criterion pass/fail; (b) trust the reduce-pass model's own citations vs. independently re-check every page_ref against real `pages` rows; (c) error on re-running an already-analyzed module vs. upsert | (a) Code: `decision = "No-Go"` if ANY criterion status is `"fail"`, else `"Go"` (conservative — a single unmet hard eligibility requirement blocks a bid regardless of how many other criteria pass); `score = round(100 * passed/total)` is informational only, never the decision driver. (b) `app.pipeline.citation_verify.verify_risk_citations` re-checks every `risk_finder` page_ref against a real `pages` row post-hoc, overriding whatever the model claims; an unresolvable citation is kept and marked `verified: false`, never dropped. (c) `document_analysis`'s own `UNIQUE(document_id, module)` constraint is honored by `reduce_pass._upsert_analysis`: update-in-place if a row exists, insert otherwise | (a) matches both real rows of golden_go_no_go.jsonl (all-pass -> Go, any-fail -> No-Go) and CLAUDE.md hard rule 3; a scoring threshold instead of any-fail-blocks would let a hard eligibility failure get silently averaged away — wrong for an MVP whose own success criterion is "zero hallucination tolerance" on eligibility facts. (b) directly operationalizes docs/SPEC.md §8's success criterion ("100% of page_refs resolve to an existing pages row") and §7's "shown as unverified, never hidden" rule — a model must never self-certify its own citation. (c) is what docs/SPEC.md §8's "re-opening an analyzed tender costs zero recomputation" success criterion requires | Domain expert wants a weighted/partial-credit Go decision instead of any-fail-blocks; citation re-verification proves too strict/lenient once compared against real pilot tender output |
| 41 | structlog output encoding on Windows | Leave stdout at its default console codepage vs. force UTF-8 | Force `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` at the top of `configure_logging()` | Real bug found during Phase 5's live validation: `run_risk_finder` crashed mid-call with `UnicodeEncodeError` the moment a real model response contained a non-ASCII character (a typographic hyphen, U+2011) that `logger.warning` tried to print — Windows' default console codepage (cp1252) can't encode it, and structlog's PrintLogger doesn't guard against that. Real tender text and real LLM prose routinely contain such characters (currency symbols, typographic punctuation), so this would have intermittently crashed any pipeline stage that logs real content on this dev machine. `errors="replace"` means logging itself can now never be the reason a call fails | Never — UTF-8 stdout is the correct behavior everywhere this runs (Linux/Docker prod targets already default to it; this only mattered because dev happens on Windows) |
| 42 | Synopsis key_dates/financials dedup key | Dedupe by (label, value, page_ref) vs. (label, value) only, keeping the first page_ref | (label, value) — collapses a fact restated across multiple pages down to one citation | Real bug found during Phase 5's live validation against the real BHEL Catering document: the same date ("Tender Dated: 17-06-2019") is genuinely restated on nearly every page of the real tender, and chunk overlap (docs/DECISIONS.md #35) multiplies that further — the original (label, value, page_ref) key produced 49 near-duplicate key_dates entries for what a human would read as ~9 distinct facts. Re-ran for real after the fix: 49 -> 12. Still a fully real, verifiable citation per fact (hard rule 7 unaffected) — only the choice of *which* real page to cite changed, not what's claimed | A real tender is found where the *first* occurrence of a repeated fact is on a less-useful page than a later one (e.g. a corrigendum superseding a cover-page date) — dedupe would then need to prefer the latest occurrence, not the first |
| 43 | Password hashing library | `passlib[bcrypt]` (as originally provisioned in requirements.txt) vs. `bcrypt` directly | `bcrypt` directly, `passlib` dropped as a dependency | Real bug: passlib 1.7.4's bcrypt backend runs a self-test (`detect_wrap_bug`) at import time that crashes with `ValueError: password cannot be longer than 72 bytes` against `bcrypt>=4.1` (the version pip actually resolves to, since passlib doesn't pin it) — a confirmed library incompatibility, not a usage mistake, reproduced independent of the input password's length. `bcrypt`'s own `hashpw`/`checkpw` work correctly called directly, so `app/core/security.py` uses those instead of routing through the broken shim | passlib ships a fixed release compatible with modern bcrypt — low priority, since bypassing it works fine and needs no further dependency |
| 44 | Phase 6 auth model | Single shared credential (no new table) vs. a minimal `users` table (new migration) vs. skip auth for now | Single shared credential — `AUTH_USERNAME`/`AUTH_PASSWORD_HASH` in config, `POST /token` issues a JWT whose `sub` is always the shared username, no per-user identity anywhere | User's explicit choice, asked directly because this needed a real decision: the spec's own DDL (§3.1, applied as-is per CLAUDE.md) has no `users` table, and Tier 2 explicitly excludes full RBAC ("JWT auth only; single internal bid-team user class") — a new table would be schema beyond the fixed DDL, exactly what CLAUDE.md's "when to stop and ask" flags. This keeps the DDL untouched and matches the spec's own "single user class" framing literally | The pilot needs to distinguish which bid-team member did what (audit trail, per-user permissions) — add a real `users` table + migration then, as a deliberate scope expansion, not a default |
| 45 | Upload-time guardrail wiring | Check content-type/size only (as before) vs. also open every upload with PyMuPDF to reject corrupt/0-page files, enforce the page ceiling, and run the "is this a tender" heuristic, all before a Document row exists | `app/guardrails/input_checks.validate_upload(pdf_bytes)` — opens the PDF once, runs all three checks, raises `DataQualityError` (caught by the same handler as everything else) on the first failure; called from `routes_ingest.py` before any DB write or S3 upload. `looks_like_a_tender` now takes an already-open `fitz.Document` (not a `Path`) so `validate_upload` opens the file once and reuses it, rather than each check re-opening | docs/SPEC.md §5 explicitly required the corrupt/0-page/over-ceiling rejections and confirming the page ceiling "before Phase 6" — this is that. Rejecting before any Document/S3 write means a bad upload costs nothing beyond the validation itself, rather than creating a `documents` row that later fails partway through ingestion | Never — this is strictly safer/cheaper than validating later in the pipeline |
| 46 | `looks_like_a_tender` false positive on an explicit disclaimer | Leave the plain substring check vs. add a negation-phrase guard | Added `NEGATION_PHRASES` (`"not a tender"`, `"not a solicitation"`, `"not a notice inviting"`, `"is not a tender"`), checked first — any match short-circuits to `False` before the positive TENDER_SIGNAL_TERMS check runs | Real bug found running the rewritten adversarial eval against the real `fixture_08_non_tender.pdf`: its own text says "It is not a tender, solicitation, or notice inviting bids of any kind" — a real non-tender document (an annual report) that explicitly disclaims tender status still contains the literal words "tender" and "notice inviting" inside its own negation, defeating a plain substring check. This is exactly the scenario the golden adversarial fixture was built to catch | The negation list starts producing its own false negatives (a real tender clause that happens to say "not a tender document" about some other referenced document) — would need real evidence before expanding/restricting the phrase list further |
| 47 | Celery queue split + concurrency | One worker pool for everything vs. separate "llm" (map/reduce/ingestion) and "default" (chunk-building) queues with different concurrency | Two queues: "llm" at `--concurrency=4`, "default" at `--concurrency=8`; `task_acks_late=True` + `worker_prefetch_multiplier=1` globally; per-chunk/per-module tasks get an explicit `soft_time_limit=600/time_limit=660`, deliberately NOT applied to `tasks_ingest.py` (still one task per whole document) | Measured for real, not assumed: firing 4 concurrent Ollama Cloud requests completed in 6.6s total wall-clock (vs. ~14.8s if fully serial) but individual call latency rose from a ~3.7s sequential baseline to 3.2-6.5s under that same load — real, partial concurrency with real per-call degradation, not infinite parallelism. `concurrency=4` is a reasoned middle point pending a fuller sweep. A single shared pool would either starve fast chunk-building behind slow LLM tasks (if sized for LLM work) or over-subscribe Ollama Cloud (if sized for cheap work) | A fuller concurrency sweep (2/4/6/8) against real chunks finds a meaningfully better throughput/latency trade-off, or Ollama Cloud's real free-tier rate limit is hit at concurrency=4 in practice (watch for 429s in logs) |
| 48 | S3/Redis explicit timeouts | Rely on library defaults vs. set explicit connect/read timeouts | boto3: `connect_timeout=10, read_timeout=60` (kept its existing `retries={"max_attempts":3,"mode":"standard"}`, which already gives backoff+jitter and a 503/SlowDown path). Redis client: `socket_connect_timeout=5, socket_timeout=5` | CLAUDE.md hard rule 10 ("every external call has a timeout") was only actually true for Ollama calls before this — S3 and Redis calls relied on each library's own unverified default. 60s read timeout on S3 accounts for a large PDF upload/download being the realistic slow case here | A real large-file upload (500MB, near the configured ceiling) times out in practice — raise `read_timeout` with evidence, not preemptively |
| 49 | Cost/token accounting | Track real dollar spend (no metered API exists here) vs. token/request volume as the cost proxy | `scripts/report_cost.py` — reports, per document: `chunks.token_count` summed across ALL built chunks (the full document's real, exact map-pass token volume, known the moment chunking runs, independent of how many chunks have actually been LLM-processed) separately from reduce-pass tokens (necessarily computed only from chunks already map-passed, since reduce input IS the map-pass's own output). `$0 required spend` is stated outright, not computed, since Ollama Cloud free tier has no payment method attached — a run either completes free or fails, there's no bill to reconcile | Real run against the 2 real documents with chunks built: the ~730 tokens/page ratio was consistent across both (Catering: 726 tokens/page over 40 pages; NHAI: 734 tokens/page over 372 pages) — a genuinely useful, evidence-based per-page cost constant for projecting any future document's volume, not a guess | A real paid tier is ever configured — this script would then need actual per-model $/token rates, which don't exist to reference honestly today |
| 50 | p95 latency target for a 500-page document (docs/SPEC.md §8) | Trust the spec's proposed "<15 min" figure vs. revise it against real measured throughput | NOT silently revised — flagged to the user with the real numbers, since this is a spec-level success criterion (not an internal tuning knob like chunk size, #35) and CLAUDE.md itself lists "an eval metric would regress" as a stop-and-ask trigger | Real data: 9 real map-pass calls across 2 real documents averaged ~38s (range 5.8-135.5s). A 500-page document plans to 125 chunks (`plan_chunk_ranges(500)`, verified). At the #47 concurrency (4), that extrapolates to roughly 125/4≈32 rounds × ~38-60s/round (accounting for per-round-max, not per-call-mean, since a round finishes when its slowest task does) ≈ 20-35 minutes for the map pass ALONE — before reduce pass, ingestion, or any vision-heavy pages. Even the smaller real 372-page NHAI document (93 chunks) lands right at ~15 min by the same math, with no margin. The 15-min figure is not achievable at current free-tier Ollama Cloud throughput and any concurrency tested so far | A real, fuller-document, fully-parallel run (not just a 9-sample extrapolation) is done end-to-end and either confirms or corrects this estimate; or the user picks a revised target / accepts a different throughput-vs-cost trade-off (e.g. a paid tier, a faster/smaller model for map pass, or accepting completeness-over-speed as the spec's own stated priority already implies) |
| 51 | First real company_profiles seed data | Wait for a full/finalized profile vs. seed now with what's confirmed, nulls for what isn't | Seeded "C4i4 Lab (Centre for Industry 4.0)" via a new, reusable `scripts/seed_company_profile.py` (upserts by `company_name`, rejects any payload field not in the real DDL) — real annual_turnover (3 years, one marked `_projected`), 3 real certifications, 11 past_projects (4 with confirmed value/year, 7 explicitly `null` value/year pending real data, never guessed), 6-state geographic_presence, 11 sectors, max_capacity_pct=85.0. Verified round-tripped correctly from live Postgres | The user shared their own real company profile document (a different system's richer MDM `company_profile` design, whose §9 explicitly frames its data as meant to seed this exact table) and asked for it to be added now, incomplete, to be completed later. Building the seed script now (rather than a one-off insert) means re-seeding as more fields are confirmed is just re-running it against an updated JSON file, not another ad-hoc script | The source document's richer schema fields (GSTIN, PAN, net worth, bank credit/BG capacity, PF/ESI registration — see its own "remaining open items") are needed for real Financial-Eligibility/Compliance criteria matching — those aren't columns in this project's frozen DDL (docs/SPEC.md §3.1) at all, so adding them would be a real schema decision requiring the same "stop and ask" treatment as Phase 6's auth table, not a silent addition |
| 52 | Langfuse deployment mode | Self-hosted (docker-compose) vs. Langfuse Cloud free tier vs. LangSmith | Self-hosted Langfuse — a new `langfuse` + `langfuse-postgres` pair of services in `docker-compose.yml` (pinned `langfuse/langfuse:2`, matching the already-pinned `langfuse==2.57.1` Python SDK), its own dedicated Postgres instance, separate from `tender_platform`'s | Asked of and confirmed by the user directly — this was flagged as an explicit open decision in the implementation plan ("confirm before Phase 8"), and for good reason: a Langfuse trace's `input`/`output` is the real prompt sent to the model, i.e. real tender clause text, the same category of business-sensitive content that's never been committed to git (#27) or sent anywhere ungoverned. LangSmith was also considered (user asked directly) — ruled out because its free/individual self-hosting is not freely available the way Langfuse's fully open-source self-host is, and self-hosting was the whole point given the data-sensitivity constraint | A genuinely air-gapped/offline deployment target needs tracing turned off entirely — `app/core/tracing.py` already no-ops cleanly when `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` are unset, so this is already a supported mode, not a gap |
| 53 | Where tracing hooks into the codebase | Instrument map_pass.py/reduce_pass.py/extract_vision.py individually vs. one hook in app.llm.client | One hook: `app.llm.client.complete_for_task` (the single function all three pipeline stages already call, hard rule 1) is wrapped in `app.core.tracing.trace_llm_call` | Every real LLM call already funnels through this one function — instrumenting it once traces map/reduce/vision automatically, with zero code changes needed in any of the three pipeline files, and zero risk of one of them being added later and forgetting to instrument it. Records one Langfuse generation per call, tagged with `task`, the model that actually served it, and whether the cloud->local fallback (#32) fired — success and failure both recorded, so a failed call is exactly as visible as a successful one, not silently missing from traces | A future need arises to trace something *below* the granularity of "one complete_for_task call" (e.g. per-retry-attempt detail) — would need a second, nested hook inside `_complete_ollama_native` itself |
| 54 | Celery task metrics for Prometheus | Custom `prometheus_client` instrumentation inside worker processes vs. a purpose-built exporter sidecar | `celery-exporter` (danihodovic/celery-exporter) as a new docker-compose service, consuming task events Celery already emits (`worker_send_task_events=True`, `task_send_sent_event=True`) off the Redis broker; Prometheus scrapes it as a second job alongside the existing FastAPI `/metrics`. Grafana auto-provisions a Prometheus datasource + one starter dashboard (HTTP rate/p95 latency, Celery task success/failure rate, Celery task duration p95 — all by task/handler name) via `grafana/provisioning/` | Custom in-worker Prometheus counters/histograms need `prometheus_client`'s multiprocess mode to be correct under Celery's prefork pool (multiple OS processes per worker, per docs/DECISIONS.md #47's `--concurrency=4/8`) — a well-known sharp edge (stale metric files, `PROMETHEUS_MULTIPROC_DIR` wiring) not worth the risk of shipping unverified, especially since Docker isn't available in this dev sandbox to test it against. A purpose-built exporter needs zero custom instrumentation code and no multiprocess handling at all | The exporter's actual metric names (assumed here: `celery_task_succeeded_total`, `celery_task_failed_total`, `celery_task_runtime_seconds_bucket`, labeled by `task`) turn out different once run for real — the dashboard JSON's PromQL queries would need correcting against its real `/metrics` output, which could not be verified in this environment (no Docker) |
| 55 | Serving page content for the citation-verification UI | Presigned S3/MinIO URLs vs. proxying page content/images through the API | Proxy through the API — `GET /documents/{id}/pages/{n}` (JSON: text/classification/confidence) and `GET /documents/{id}/pages/{n}/image` (raw PNG bytes) | No route existed to fetch a single page's content at all — a real gap, since the citation-verification UI (docs/SPEC.md's actual HITL mechanism, the whole reason `page_ref` exists on every fact) is non-functional without one. Presigned URLs were considered but rejected: MinIO's `s3_endpoint_url` inside docker-compose (`http://minio:9000`) is a docker-network-internal hostname a browser on the host can't resolve — a well-known real gotcha, and not one that could be verified without Docker in this dev sandbox. Proxying avoids the whole class of problem: the browser only ever talks to the already-correctly-exposed API. Page images are small (100 DPI per docs/DECISIONS.md #30, ~191KB), so the extra hop through the API is cheap | The pilot's image volume/frequency makes API-proxy bandwidth a real bottleneck — revisit presigned URLs then, with a real fix for the internal-vs-external hostname mismatch (e.g. a separate public MinIO endpoint setting) |
| 56 | CORS configuration (backend) | None (not noticed until the frontend existed) vs. an explicit allowlist | `CORSMiddleware` in `main.py`, origins from a new `settings.cors_allowed_origins` (comma-separated, `.env`-driven) — defaults to `http://localhost:3000`, docker-compose's `api` service also allows `:3002` (the dockerized frontend's host-mapped port) | Real gap found while driving the newly-built frontend against the real backend in an actual browser: with no CORS middleware at all, every cross-origin request from the frontend (`:3000`) to the API (`:8000`) would be silently blocked by the browser itself — no server-side error, nothing in the backend's own logs, just requests that never arrive. `next build`/`tsc`/ESLint/pytest all pass with this gap present; only a real browser against a real running backend exposes it, which is exactly why that verification step was taken even though it wasn't strictly required by the task | The frontend is ever deployed to a real (non-localhost) origin — that origin needs adding to `CORS_ALLOWED_ORIGINS` in the real deployment's `.env` |
| 57 | Next.js frontend scaffold + two real bugs found driving it in a real browser | — | Next.js 16 (App Router) + Tailwind v4, `create-next-app` scaffold, single-shared-credential auth via `localStorage` + a client-side `AuthGuard`, `lib/api.ts` as the one place that calls the backend (mirrors `app/llm/client.py`'s "one file" pattern from the backend). `CitationLink` is the actual HITL UI (`docs/SPEC.md`'s HITL note) | Verified end-to-end in a real headless browser (Playwright — `chromium-cli` unavailable in this environment) against the real running backend, real Postgres data, and a real (directly-minted, since Redis wasn't available locally to exercise the real rate-limited `/token` flow) JWT: login page, unauthenticated redirect, real CORS preflight (#56), real document list, real per-module tabs, and a real citation click showing real BHEL tender page text. Two real bugs surfaced only by this, not by `next build`/`tsc`/ESLint: (a) `AuthGuard`'s redirect effect fired on the SSR-matched first render (`authed=false`) before `useSyncExternalStore` could correct it to the real client value, sending an actually-logged-in user back to `/login` — fixed by reading `getToken()` directly inside the effect instead of branching on the render-time `authed` value; (b) `CitationLink`'s modal, portal-free, rendered its `<div>`/`<pre>` content as a literal DOM child wherever the link itself was used (a `<p>` in `RiskList`/`SynopsisView`, a `<td>` in `GoNoGoCard`) — invalid HTML (block elements inside an inline/prose element), caught as a real React hydration-error console warning — fixed with `createPortal(modal, document.body)` | Neither — both are now the correct, tested pattern for this app; noted here as the concrete payoff of "start the dev server and use the feature in a browser," not a hypothetical |
| 58 | CI coverage for the frontend | Backend-only CI (as it was through Phase 7) vs. add a frontend job now that `frontend/` exists | New `frontend-lint-and-build` job in `.github/workflows/ci.yml` — `npm ci`, ESLint, `npm run build` (which includes `tsc` type-checking) | The frontend is now a first-class part of this repo with the same real bugs found in it this session (docs/DECISIONS.md #56-57) as anywhere in the backend — leaving it unchecked by CI would mean the next change to it ships unverified, the exact gap this project has otherwise never allowed since Phase 0 | Frontend test coverage (not just lint/build) is added — a real Playwright suite, mirroring how this phase's manual browser verification was done, rather than one more manual pass next time |
